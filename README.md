# Self-Optimizing-Neural-Network
This PyTorch project implements a self-pruning neural network using custom gated linear layers. By applying L1 regularization to sigmoid gates, the model dynamically removes redundant weights during training. It achieves 90%+ sparsity on CIFAR-10 while maintaining high classification accuracy.
